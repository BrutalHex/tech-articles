apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace is also set by the stack overlay; kept here for staged `make apply`.
namespace: agentic-chain

resources:
  - postgres.yaml
  - chroma.yaml
