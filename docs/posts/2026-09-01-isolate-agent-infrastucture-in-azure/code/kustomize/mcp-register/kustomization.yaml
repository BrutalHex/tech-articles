apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: agentic-chain

resources:
  - job.yaml

configMapGenerator:
  - name: mcp-register
    files:
      - register.py
    options:
      disableNameSuffixHash: true
