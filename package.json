{
  "scripts": {
    "docs:dev": "node scripts/generate-posts-index.mjs && vitepress dev docs",
    "docs:build": "node scripts/generate-posts-index.mjs && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}